#include <tunables/global>

profile documentarian flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  network inet stream,
  network inet6 stream,

  / r,
  /app/ r,
  /app/.venv/bin/** rix,
  /app/.venv/** mr,
  /app/** r,
  /bin/** rix,
  /data/ rw,
  /data/** rwk,
  /dev/null rw,
  /dev/random r,
  /dev/urandom r,
  /etc/** r,
  /lib/** r,
  /lib64/** r,
  /proc/** r,
  /read_options.py r,
  /run.sh rix,
  /sys/devices/system/cpu/** r,
  /tmp/ rw,
  /tmp/** rwk,
  /usr/** rix,
}
